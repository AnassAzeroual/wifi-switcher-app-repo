import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.wifiswitcher',
  appName: 'wifi-switcher',
  webDir: 'dist/browser'
};

export default config;
